//~---按位取反
int a = 0;//00000000 00000000 00000000 00000000
int b = ~a;
printf("%d\n",b);//此时输出b值为-1

//后置++,先使用，再++
int a=10;
int b =a++;//先把a赋值给b,b再执行++操作返回给a
//输出a=11,b=10

//前置++，先++，再使用
int a=10;
int b =++a;//先把a执行++操作，再把a赋值给b
//输出a=11,b=11

//()--强制类型转换
int a = (int)3.14;//double强制转化成int
